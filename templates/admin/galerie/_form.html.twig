{{ form_start(form) }}
<div class="row">
    <div class="offset-1 col-10 offset-md-3 col-md-6">{{ form_row(form.imageFile) }}</div>
</div>
{{ form_rest(form) }}
<div class="row">
    <div class="offset-2 col-8 offset-md-4 col-md-4 py-2">
        <button class="col-12 btn-sm bg-button-purple text-white border-0">{{ button|default('Enregistrer') }}</button>
    </div>
</div>
{{ form_end(form) }}