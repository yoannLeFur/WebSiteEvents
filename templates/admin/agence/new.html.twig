{% extends 'base.html.twig' %}
{% block title 'Description' %}

{% block body %}
    <div>
        {{ include('layout/_header-nav-admin.html.twig') }}
    </div>
    {# Connexion #}
    <div id="login" class="section-height">
        <div class="offset-0 col-12 py-10">
            <div class="row m-0">
                <h2 class="form-padding m-auto">Crée votre nouvelle description</h2>
            </div>
            {{ include('admin/agence/_form.html.twig', {form: form, button: 'Créer'}) }}
        </div>
    </div>
{% endblock %}